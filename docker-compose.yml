# → Architecture & Build by DocSynapse
# Intelligent by Design. Crafted for Humanity.

version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - STORAGE_PATH=/app/storage
      - CORS_ORIGINS=http://localhost:3000,http://localhost:3001
    volumes:
      - ./storage:/app/storage
      - ./test_docmemory_storage:/app/test_docmemory_storage
    # TODO: Add healthcheck
    # TODO: Add restart policy
    # TODO: Add resource limits

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    # TODO: Add nginx for production
    # TODO: Add healthcheck

  # TODO: Add PostgreSQL service when migrating
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: docmemory
  #     POSTGRES_USER: docmemory
  #     POSTGRES_PASSWORD: changeme
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # TODO: Add Redis service for caching
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

# volumes:
#   postgres_data:

